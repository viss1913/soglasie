# 🎯 Простая инструкция для копирования в фронтенд

## Что копировать:

1. **`figma-integration-example.js`** - основной файл (обязательно)
2. **`figma-integration-example.html`** - пример для теста (опционально)

## Минимальный пример использования:

```javascript
// 1. Импортируйте файл
import { FigmaAPIService, figmaPageToHTML } from './figma-integration-example.js';

// 2. Используйте ваш токен и file key
const token = 'figd_ваш_токен'; // который вы создали
const fileKey = 'ваш_file_key'; // из URL Figma

// 3. Загрузите дизайн
const figmaService = new FigmaAPIService(token);
const file = await figmaService.getFile(fileKey);
const page = file.document.children[0]; // первая страница

// 4. Преобразуйте в HTML и покажите
const htmlContainer = figmaPageToHTML(page);
document.getElementById('container').appendChild(htmlContainer);
```

## Для React/Vue:

Просто импортируйте и используйте:

```javascript
import { FigmaAPIService, figmaPageToHTML } from './services/figma-integration-example.js';
```

Все готово! Файл сам работает с Figma API.

