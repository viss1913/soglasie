# 🎨 Интеграция Figma для страницы результатов

## 📋 Что сделано

✅ Полная интеграция с Figma API  
✅ Автоматическая загрузка дизайна из Figma  
✅ Автоматический рендеринг дизайна в React компоненты  
✅ Подстановка данных расчета в дизайн  

## 🚀 Быстрый старт

### 1. Настройте переменные окружения

Создайте файл `.env` в корне проекта:

```env
VITE_FIGMA_TOKEN=YOUR_FIGMA_TOKEN_HERE
VITE_FIGMA_FILE_KEY=HIc2F0OeTuvafJNSTKMm3E
```

**Важно:** Файл `.env` уже добавлен в `.gitignore` и не будет закоммичен.

### 2. Запустите приложение

```bash
npm install
npm run dev
```

### 3. Протестируйте

1. Пройдите все шаги расчета
2. На странице результатов автоматически загрузится дизайн из Figma
3. Данные из расчета будут подставлены в дизайн

## 📁 Структура файлов

```
src/
├── services/
│   └── figmaService.ts          # Сервис для работы с Figma API
├── utils/
│   ├── figmaInspector.ts        # Утилиты для анализа структуры
│   ├── loadFigmaDesignStructure.ts  # Загрузка структуры дизайна
│   └── viewFigmaStructure.ts    # Просмотр структуры в консоли
├── components/
│   ├── FigmaDesignRenderer.tsx  # Рендерер дизайна из Figma
│   ├── ResultDesignView.tsx     # Компонент страницы результатов
│   └── ResultPage.tsx           # Главная страница результатов
```

## 🔍 Как это работает

### 1. Загрузка структуры

При открытии страницы результатов:
- Загружается структура дизайна из Figma через API
- Извлекаются все элементы, стили, цвета, шрифты
- Определяется основной фрейм для рендеринга

### 2. Рендеринг

Компонент `FigmaDesignRenderer`:
- Преобразует структуру Figma в React компоненты
- Сохраняет стили (цвета, шрифты, отступы)
- Воссоздает макет и расположение элементов

### 3. Подстановка данных

Автоматически подставляются данные из расчета:
- Имя клиента
- Цели и суммы
- Планы и рекомендации

## 🛠️ Отладка

### Просмотр структуры дизайна

В консоли браузера выполните:

```javascript
viewFigmaStructure()
```

Или откройте раздел "Показать информацию о дизайне" на странице результатов.

### Проверка загруженной структуры

В консоли браузера доступна глобальная переменная:

```javascript
window.__FIGMA_STRUCTURE__
```

## 📝 Референс дизайна

**URL Figma:** https://www.figma.com/design/HIc2F0OeTuvafJNSTKMm3E/Фронт

**File Key:** `HIc2F0OeTuvafJNSTKMm3E`

## 🔧 Настройка

### Изменение File Key

В файле `src/components/ResultPage.tsx`:

```typescript
const FIGMA_FILE_KEY = import.meta.env.VITE_FIGMA_FILE_KEY || 'HIc2F0OeTuvafJNSTKMm3E';
```

Или через переменную окружения в `.env`:

```env
VITE_FIGMA_FILE_KEY=ваш_новый_file_key
```

### Выбор конкретной страницы/фрейма

В `src/utils/loadFigmaDesignStructure.ts` можно настроить выбор конкретной страницы или фрейма по имени.

## ⚠️ Важные замечания

1. **Токен доступа** должен храниться только в `.env` файле
2. **File Key** должен быть доступен через токен
3. При изменении дизайна в Figma изменения автоматически подхватываются
4. Для продакшена рекомендуется кэширование структуры дизайна

## 🐛 Решение проблем

### Ошибка загрузки дизайна

1. Проверьте токен в `.env` файле
2. Убедитесь, что файл доступен по токену
3. Проверьте File Key в URL Figma

### Дизайн не отображается

1. Откройте консоль браузера (F12)
2. Проверьте ошибки загрузки
3. Убедитесь, что структура дизайна загрузилась

### Неправильные стили

Текущая версия использует базовое преобразование стилей. Для точного соответствия дизайну может потребоваться доработка компонента `FigmaDesignRenderer`.



